language: java
jdk:
  - openjdk8
    
cache:
  directories:
    - '$HOME/.m2/repository'

before_install:
  - chmod +x mvnw
  
script:
 - "./mvnw clean install cobertura:cobertura"

after_success:
  - bash <(curl -s https://codecov.io/bash)
