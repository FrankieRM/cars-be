language: java
sudo: false #faster builds
jdk:
  - oraclejdk8
    
cache:
  directories:
    - '$HOME/.m2/repository'

before_install:
  - chmod +x mvnw
  
before_script:
  - _JAVA_OPTIONS=
  
script:
 - "./mvnw clean install cobertura:cobertura"
